# JitPack 빌드 환경 설정 (webviewlib/commonlib 과 통일)
# AGP 8.6 은 JDK 17 이상을 요구하므로 OpenJDK 17 로 빌드한다.
jdk:
  - openjdk17

# databaselib 라이브러리 모듈만 게시(app 은 application 이라 게시 대상 아님)
install:
  - ./gradlew :databaselib:publishToMavenLocal
